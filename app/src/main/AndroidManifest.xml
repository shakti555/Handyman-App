<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.handyman.handyman">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/icon"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/icon"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".MainActivity"
            android:theme="@style/Theme.AppCompat.DayNight.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".WelcomeActivity" />
        <activity android:name=".HandyManLogActivity" />
        <activity android:name=".MyBookingActivity" />
        <activity android:name=".HRegistrationActivity" />
        <activity android:name=".CRegistrationActivity" />
        <activity android:name=".CustomerLogActivity" />
        <activity android:name=".CMobileActivity" />
        <activity android:name=".VerifyOTPActivity" />
        <activity
            android:name=".CdashBoardActivity"
            android:label="@string/title_activity_cdash"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".MandyManListActivity" />
        <activity android:name=".BookServiceActivity" />
        <activity android:name=".BookCustActivity" />
        <activity
            android:name=".HandymanDashBoardActivity"
            android:label="@string/title_activity_hdash_board"
            android:theme="@style/AppTheme.NoActionBar"></activity>
    </application>

</manifest>